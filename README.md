create virtualenv by => virtualenv venv

active virtualenv by => mac&linux => sourse ./venv/bin/activate windows => .\venv\Scripts\activate

install requirements => pip install -r requirements.txt

run django app

python mamange.py runserver

